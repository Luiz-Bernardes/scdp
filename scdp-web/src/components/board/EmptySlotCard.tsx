import { Card } from "@/components/ui/Card";

export function EmptySlotCard() {
  return (
    <Card className="min-h-[140px] flex items-center justify-center text-gray-400">
      Slot livre
    </Card>
  );
}